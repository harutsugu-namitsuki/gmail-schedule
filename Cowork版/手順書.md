# 📋 手順書：Cowork スケジュールタスク + Computer Use による「毎朝のトップファーム発信ダイジェスト生成」

## このドキュメントの位置づけ

本書は、**Claude Cowork のスケジュールタスク**（Desktop local task）と **Chrome ブラウザ操作 / Computer Use** を組み合わせて本リポジトリを運用するための**セットアップ & 運用手順書**。

前回版（Anthropic Cloud + fetch）は主要ドメインが HTTP 403 で全滅し、プロジェクトがクローズされた。本書はその再設計版であり、**記事取得を Chrome 実ブラウザ経由にすることで Bot 判定を回避**し、ユーザーのログイン済みセッションを活用する。

> ⚠️ **STEP 0 に進む前に必ず [前提検証テスト手順書.md](前提検証テスト手順書.md) を実施すること。** 同書が PASS しない限り、本書の STEP 0 以降に進んではならない。これは前回失敗の直接の再発防止である。

**各コンポーネントの詳細仕様は「実ファイル」と「設計書」を正とし、本書では埋め込みコードを持たない。**

| 役割 | 正となるファイル |
|---|---|
| プロジェクト基本方針 | [CLAUDE.md](CLAUDE.md) |
| 前回失敗の振り返り | [振り返り報告書.md](振り返り報告書.md) |
| 失敗防止ガードレール | [失敗防止ガードレール.md](失敗防止ガードレール.md) |
| 前提検証テスト | [前提検証テスト手順書.md](前提検証テスト手順書.md) |
| 移行設計 | [移行設計書.md](移行設計書.md) |
| 初回セットアップチェック | [初回セットアップチェックリスト.md](初回セットアップチェックリスト.md) |
| タスク実行本体 | [tasks/daily-email-scan.md](tasks/daily-email-scan.md) |
| レポートフォーマット | [templates/report-template.md](templates/report-template.md) ＋ [docs/日次レポートテンプレート設計書.md](docs/日次レポートテンプレート設計書.md) |
| インサイトフォーマット | [templates/insight-universal.md](templates/insight-universal.md) ＋ [docs/インサイトデータベース設計書.md](docs/インサイトデータベース設計書.md) |
| 検索キーワード設定 | [config/search-keywords.json](config/search-keywords.json) |
| インサイト生成設定 | [config/insight-config.json](config/insight-config.json) |
| ドメインアクセスレベル | [config/domain-access-levels.json](config/domain-access-levels.json) |
| 前提検証対象ドメイン | [config/test-domains.json](config/test-domains.json) |

---

## 全体アーキテクチャ

```
┌───────────────────────────────────────────────────────────────┐
│  Cowork スケジュールタスク（ユーザーの Windows PC・毎朝実行）  │
│                                                                │
│  ① tasks/daily-email-scan.md の指示に従って実行                │
│       ↓                                                        │
│  ② 設定ファイル群を読み込む                                    │
│     (search-keywords / insight-config /                        │
│      domain-access-levels / business-hypotheses)               │
│       ↓                                                        │
│  ③ Gmail コネクター（第1層）で受信メールを検索（read-only）   │
│       ↓                                                        │
│  ④ 各メールのURLを Chrome ブラウザ操作（第2層）で取得          │
│     （ユーザーのログイン済みセッションを利用）                 │
│     ※ Chrome で取得不可の場合は Computer Use（第3層）          │
│     ※ それでも取得不可の場合は URL + 📌手動確認                │
│       ↓                                                        │
│  ⑤ 匿名化処理（実行者の氏名・メアドを [REDACTED] 化）          │
│       ↓                                                        │
│  ⑥ 日次レポート生成（5層構造：Top Insights / Actions /        │
│     Knowledge 8バケット / Raw Data 全文展開 / 英単語10選）     │
│     → reports/YYYY-MM-DD-consulting-digest.md                  │
│       ↓                                                        │
│  ⑦ 優先度高のインサイトを 1ファイル単位で永続化                │
│     → insights/YYYY-MM-DD-NNN.md                               │
│       ↓                                                        │
│  ⑧ git commit & push                                           │
└───────────────────────────────────────────────────────────────┘

前提: Windows PC 起動中 & Claude Desktop 開いている & Chrome でログイン済み
```

---

## STEP −1：前提検証テスト（最優先・省略禁止）

**以下の STEP 0 以降に進む前に、必ず [前提検証テスト手順書.md](前提検証テスト手順書.md) を実施してください。**

同書の Layer 1 〜 Layer 3 の検証で GO 判定が出なければ、本手順書の STEP 0 以降に進んではいけません。前回はこのゲートが無かったために、核心的前提の崩壊を最後まで検知できず、構築物全てが無駄になりました。

---

## STEP 0：リポジトリの準備

### 0-1. 「タスクコード固め」方式の採用

Cowork スケジュールタスクも、指定したローカルフォルダ（working folder）のファイルを読み取れる。本リポジトリは**タスクの詳細指示を tasks/ templates/ config/ に固め、スケジュールタスクのプロンプト欄は「tasks/daily-email-scan.md を読んで実行せよ」という短い委譲だけにする**方式を採用している。

#### メリット

| メリット | 説明 |
|---|---|
| **タスク定義のバージョン管理** | Git で変更履歴が残る |
| **プロンプトの安定化** | スケジュールタスクのプロンプト欄は短く保ち、詳細はファイルに委譲 |
| **再利用・複製が容易** | 別のキーワードで同様のタスクを作りたい場合、ファイルをコピーして修正するだけ |
| **レビュー可能** | Pull Request でタスク定義の変更をレビューできる |

### 0-2. 現行のリポジトリ構成

```
gmail-schedule/Cowork版/
├── README.md                           ← プロジェクト概要
├── CLAUDE.md                           ← 基本方針（絶対遵守ルール）
├── 手順書.md                           ← 本ファイル
├── 前提検証テスト手順書.md             ← STEP −1（最優先）
├── 失敗防止ガードレール.md             ← 運用時の禁止事項
├── 振り返り報告書.md                   ← 前回失敗の振り返り
├── 移行設計書.md                       ← Cloud → Cowork 移行設計
├── 初回セットアップチェックリスト.md   ← セットアップ漏れ防止
├── テスト関連提案書.md                 ← Chrome 経由テスト方針
├── tasks/
│   └── daily-email-scan.md             ← タスク定義（実行本体）
├── config/
│   ├── search-keywords.json            ← 監視対象送信者
│   ├── insight-config.json             ← インサイト生成設定
│   ├── domain-access-levels.json       ← ドメイン別アクセス可否
│   ├── business-hypotheses.md          ← 検証中の4仮説
│   ├── test-domains.json               ← 前提検証用URL（3層）
│   └── domain-test-results.md          ← テスト結果記録（自動生成）
├── templates/
│   ├── report-template.md              ← 日次レポート（5層構造）
│   └── insight-universal.md            ← インサイト
├── docs/
│   ├── 日次レポートテンプレート設計書.md
│   └── インサイトデータベース設計書.md
├── reports/                            ← 日次レポート出力先（自動生成）
└── insights/                           ← インサイト出力先（自動生成）
```

### 0-3. 各ファイルの役割

各ファイルの最新仕様は、冒頭の対応表から実ファイルを直接参照すること。

### 0-4. domain-access-levels.json の運用

ドメインごとに **Chrome ブラウザ操作によるアクセス可否**を分類：

| 分類 | 意味 | 運用時の扱い |
|---|---|---|
| 🟢 `full_access` | Chrome 経由で本文が取得できる | 通常通り要約 + 原文引用 + 和訳 |
| 🟡 `partial_access` | 冒頭/目次のみ取得可能 | 取得できた範囲で要約 + 「⚠️ 一部のみ取得可能」と注記 |
| 🔴 `no_access` | Chrome でも取れない（構造的ブロック） | 何も試みず、リンクのみ記載 + 「📌 手動確認」と明記 |

**初期値は空（未分類）**。前提検証テスト手順書の実測結果に基づいて初期化する。前回版では事前予想で `no_access` に埋めていたが、Cowork 移行で結果が変わる可能性があるため**必ず実測**すること。

---

## STEP 1：Claude Desktop のセットアップ（Windows）

### 1-1. インストール

1. https://claude.com/download から **Windows 版**（.exe）をダウンロード
2. インストーラーを実行し、画面の指示に従う
3. スタートメニューから Claude を起動し、Pro アカウントでログイン

> ⚠️ **Windows 固有の注意事項:**
> - 初回起動時に **Virtual Machine Platform のインストール**を求められる場合があります。画面の指示に従ってインストールし、指示があれば PC を再起動してください。
> - セキュリティソフト（ウイルスバスター、ノートン等）が Cowork の仮想マシンをブロックする場合があります。動作が不安定な場合は Claude Desktop を例外設定に追加してください。
> - 作業フォルダのパスに日本語やスペースが含まれる場合は、STEP 1.5 でシンボリックリンクを作成して対処します（本プロジェクトの実ファイルは OneDrive 上の日本語パスにありますが、シンボリックリンクで回避済みです）。

### 1-2. Computer Use の有効化

1. Claude Desktop を開く
2. **Settings > General**（Desktop app セクション）を開く
3. **Computer use** トグルを **ON** にする

> ✅ **Windows ではこれだけでセットアップ完了です。**
> macOS では追加の権限付与（Accessibility / Screen Recording）が必要ですが、**Windows では不要**です。
>
> 公式: *"On Windows, the toggle takes effect immediately and setup is complete."*
> — https://code.claude.com/docs/en/desktop

参照: https://support.claude.com/en/articles/14128542

### 1-3. Keep computer awake の設定

**方法A: Claude Desktop の設定（必須）**

1. Settings > General
2. **Keep computer awake** トグルを **ON** にする

**方法B: Windows の電源設定（併用推奨）**

1. 設定 > システム > 電源とバッテリー（またはスリープ）
2. 「画面の電源を切る」→ 任意（画面OFFでもタスクは動く）
3. 「スリープ状態にする」→ **なし**

> ⚠️ **ノートPCの場合**：蓋を閉じるとスリープするため、蓋は開けたままにするか、コントロールパネル > 電源オプション > **「カバーを閉じたときの動作」を「何もしない」**に設定してください。
> 参照: https://code.claude.com/docs/en/desktop-scheduled-tasks

### 1-4. Claude in Chrome 拡張機能のインストール

記事取得の第一選択（Chrome ブラウザ操作）に必要。

1. Chrome ウェブストアから「Claude in Chrome」を検索してインストール
2. Claude Desktop と連携（拡張機能側のプロンプトに従う）
3. Chrome を再起動

### 1-5. 対象サイトへの事前ログイン

Chrome ブラウザで以下のサイトに**手動でログイン**しておく。Claude はこのログイン済みセッション（Cookie）を利用して記事を取得する。

| サイト | URL | 備考 |
|---|---|---|
| McKinsey | https://www.mckinsey.com/ | 無料登録で Insights 閲覧可 |
| BCG | https://www.bcg.com/ | 無料登録で Publications 閲覧可 |
| Gmail | https://mail.google.com/ | HTML からのURL抽出フォールバック経路（[tasks/daily-email-scan.md](tasks/daily-email-scan.md) 手順4-1 第二選択）として必要 |

> ⚠️ **Claude に認証情報（パスワード等）を直接渡さない。** Chrome で事前にログインしておけば、Claude はそのセッションを利用する。
>
> 公式: "Claude is trained to avoid... inputting sensitive data"
> — https://support.claude.com/en/articles/14128542

> **注**: HBR / Financial Times / 日経 は本プロジェクトの対象から除外済み（トップファーム発信のダイジェスト生成に不要との判断）。対象を広げる場合はここに追記し、`config/domain-access-levels.json` にも実測後に登録する。

### 1-6. Gmail コネクターの設定

1. [claude.ai/settings/connectors](https://claude.ai/settings/connectors) にアクセス
2. **Google Workspace（Gmail）** を追加
3. OAuth 認証を完了

> **権限について**: Google の OAuth 画面では「メール送信」権限が表示されますが、これは Google 側の仕様です。Claude は読み取りとドラフト作成のみで、送信は行いません。
>
> 参照: [Use Google Workspace connectors](https://support.claude.com/en/articles/10166901)

---

## STEP 1.5：GitHub リポジトリの準備

Cowork の local task は GitHub 連携が必須ではなく、**working folder** に指定したローカルフォルダで `git push` を実行する。


### 1-5-1. シンボリックリンクの作成（日本語パスへの対処）

OneDrive + 日本語フォルダ名のパス（例: `C:\Users\...\ドキュメント\GitHub\gmail-schedule`）は、一部のツールでエラーが出る場合がある。**シンボリックリンク**を使って短いASCIIパスの別名を作り、Claude Desktop にはそちらを指定することで回避する。

> **シンボリックリンクとは**: ファイルの実体は元の場所（OneDrive 上）に置いたまま、別のパスからアクセスできるようにする Windows の機能。ファイルのコピーではなく「別名」なので、容量も増えず、OneDrive 同期もそのまま動く。

#### 手順1：実体パスの確認

1. エクスプローラーで `gmail-schedule` の `Cowork版` フォルダを開く
2. アドレスバーをクリックすると、フルパスが表示される
3. そのパスをメモ帳にコピーしておく

```
例: C:\Users\YuenongMei(梅月濃)\OneDrive - 株式会社Dirbato\ドキュメント\GitHub\gmail-schedule\Cowork版
```

> ⚠️ **このパスが自分の環境と一致しているか必ず確認すること。** ユーザー名や OneDrive のフォルダ名が微妙に違う場合がある。手順5のコマンドにはここで確認した正確なパスを使う。

#### 手順2：管理者権限でコマンドプロンプトを開く

シンボリックリンクの作成には**管理者権限が必須**。通常のコマンドプロンプトでは「権限がありません」エラーになる。

1. タスクバーの **検索ボックス**（虫眼鏡アイコン）をクリック
2. **`cmd`** と入力する
3. 検索結果に「コマンド プロンプト」が表示される
4. **「管理者として実行」** をクリック（右側に表示される、または右クリック → 管理者として実行）
5. 「このアプリがデバイスに変更を加えることを許可しますか？」→ **「はい」** をクリック

開いたウィンドウのタイトルバーに **「管理者: コマンド プロンプト」** と表示されていれば成功。

```
管理者: コマンド プロンプト          ← これが表示されていること
C:\Windows\System32>               ← カーソルがここにある
```

> ⚠️ タイトルバーに「管理者」がなければ、管理者権限で開けていない。手順2をやり直すこと。

#### 手順3：C:\repos フォルダを作成する

シンボリックリンクの親フォルダを先に作る。

```cmd
mkdir C:\repos
```

- 何もメッセージが出ない or「ディレクトリが作成されました」→ 成功
- 「既に存在します」→ 問題なし、次へ進む

#### 手順4：既存のシンボリックリンクがないか確認する

過去に作りかけたものが残っていると失敗するので、先に確認する。

```cmd
dir C:\repos
```

- `gmail-schedule` が表示されない → 問題なし、手順5へ
- `gmail-schedule` が表示される → 以下で削除してから手順5へ

```cmd
rmdir C:\repos\gmail-schedule
```

#### 手順5：シンボリックリンクを作成する

以下のコマンドを **1行で** 入力して Enter を押す。

```cmd
mklink /D C:\repos\gmail-schedule "C:\Users\YuenongMei(梅月濃)\OneDrive - 株式会社Dirbato\ドキュメント\GitHub\gmail-schedule\Cowork版"
```

> **コピー＆ペーストの方法:**
> 1. 上のコマンドをブラウザ上で選択してコピー（Ctrl+C）
> 2. コマンドプロンプトのウィンドウ上で **右クリック** → 貼り付けられる
> 3. Enter を押す

> ⚠️ **手順1で確認したパスが上記と異なる場合は、ダブルクォーテーション内を自分の正確なパスに置き換えること。**

**成功した場合の表示:**

```
C:\repos\gmail-schedule <<===>> C:\Users\YuenongMei(梅月濃)\OneDrive - ... にシンボリック リンクが作成されました
```

**失敗した場合の対処:**

| エラーメッセージ | 原因 | 対処 |
|---|---|---|
| `この操作を実行するための十分な特権がありません` | 管理者権限で開いていない | 手順2をやり直す |
| `既に存在するファイルを作成することはできません` | `C:\repos\gmail-schedule` が既にある | 手順4で削除してからやり直す |
| `指定されたパスが見つかりません` | リンク先のパスが間違っている | 手順1で確認したパスと一致しているか確認。パスをダブルクォーテーションで囲んでいるか確認 |

#### 手順6：動作確認

**6a. ファイルが見えるか確認:**

```cmd
dir C:\repos\gmail-schedule
```

`CLAUDE.md`、`tasks`、`config` 等のファイル・フォルダが表示されれば成功。

**6b. git が動くか確認:**

```cmd
cd C:\repos\gmail-schedule
git status
```

`On branch main` 等のブランチ情報が表示されれば、git リポジトリとして正常に認識されている。

**6c. git の長いパス設定を有効化:**

```cmd
git config --global core.longpaths true
```

何もメッセージが出なければ成功。

#### 手順7（推奨）：git push が通るか確認

シンボリックリンク経由で push が正常に動作するか、テストファイルで確認する。

```cmd
cd C:\repos\gmail-schedule
echo test > .linktest
git add .linktest
git commit -m "symlink test"
git push
```

GitHub に push できたら、テストファイルを削除して後片付け：

```cmd
git rm .linktest
git commit -m "cleanup symlink test"
git push
```

> ⚠️ `git push` で認証エラーが出る場合は、Git for Windows の認証設定（GitHub CLI `gh auth login` または Git Credential Manager）を確認すること。

#### 全体の流れ

```
手順1: エクスプローラーで実体パスを確認・コピー
  ↓
手順2: 「cmd」検索 → 「管理者として実行」
  ↓
手順3: mkdir C:\repos
  ↓
手順4: dir C:\repos で既存確認（あれば rmdir）
  ↓
手順5: mklink /D コマンドを実行
  ↓
手順6: dir / git status / git config で動作確認
  ↓
手順7: git push テスト（推奨）
  ↓
完了 → STEP 1.5-2 へ進む
```

所要時間：**5〜10分**

---

## STEP 2：Cowork スケジュールタスクの作成

### 2-1. 方法A：Cowork UI から作成（推奨）

1. Claude Desktop を開き、左サイドバーの **「Scheduled」** をクリック
2. 右上の **「+ New task」** をクリック
3. **「New local task」** を選択
4. 以下を設定：

| 設定項目 | 入力内容 |
|---|---|
| **Task name** | `daily-consulting-digest` |
| **Description** | 毎朝のトップファーム発信ダイジェスト生成 |
| **Prompt** | 下記「プロンプト（コピー用）」を貼り付け |
| **Frequency** | **Daily** → 時刻ピッカーで **05:30** を選択 |
| **Model** | 利用可能な最新モデルを選択 |
| **Working folder** | STEP 1.5 で作成したシンボリックリンクのパス（`C:\repos\gmail-schedule`）を指定する |

5. **Save** をクリック

参照: https://support.claude.com/en/articles/13854387 / https://code.claude.com/docs/en/desktop-scheduled-tasks

### 2-2. 方法B：チャットから `/schedule` で作成

1. Cowork で新しいタスクを開く
2. チャット入力欄に `/schedule` と入力
3. Claude の質問に答えてタスクを設定

### 2-3. プロンプト（コピー用）

[CLAUDE.md](CLAUDE.md) の「スケジュールタスクのプロンプト（コピー用）」セクションを丸ごと使用する。プロンプト本体は本手順書には埋め込まない（二重管理防止）。

### 2-4. 初回の権限承認（重要）

タスク作成後、必ず **「Run now」で手動実行** し、表示される権限プロンプトで各ツール・アプリに対して **「Always allow」** を選択する。

> "To avoid stalls, click Run now after creating a task, watch for permission prompts, and select 'always allow' for each one."
> — https://code.claude.com/docs/en/desktop-scheduled-tasks

承認が必要になる対象：

- Gmail コネクター
- Claude in Chrome 拡張（Chrome ブラウザ操作）
- Computer Use（画面操作 / フォールバック用）
- ローカルファイル書き込み（reports/ insights/）
- git コマンド（commit, push）

**1つでも「Ask each time」のまま残すと、翌朝の自動実行が権限プロンプトで停止する。**

### 2-5. 実行時刻のずれ（公式仕様）

> "Each task gets a fixed delay of up to 10 minutes after the scheduled time to stagger API traffic. The delay is deterministic: the same task always starts at the same offset."
> — https://code.claude.com/docs/en/desktop-scheduled-tasks

5:30 設定の場合、実際の実行は 5:30〜5:40 の間になる。

### 2-6. 見逃し補填（キャッチアップ）

> "When the app starts or your computer wakes, Desktop checks whether each task missed any runs in the last seven days. If it did, Desktop starts exactly one catch-up run for the most recently missed time."
> — https://code.claude.com/docs/en/desktop-scheduled-tasks

PCがスリープしていた場合、起動後に最新1回分だけ自動実行される。

---

## STEP 3：動作確認

### 3-1. 事前：前提検証テスト（初回のみ）

[前提検証テスト手順書.md](前提検証テスト手順書.md) に従い、Chrome 経由での記事取得が実測で動作することを確認する。この結果で `config/domain-access-levels.json` を初期化する。

**本テストが PASS しない限り、STEP 3-2 以降に進んではならない。**

### 3-2. 手動テスト実行

1. 左サイドバーの **「Scheduled」** をクリック
2. `daily-consulting-digest` タスクを選択
3. **「Run now」** をクリック
4. **権限プロンプトが表示されたら「Always allow」を選択**（初回のみ）
5. 実行完了を待つ（10〜30分程度。Chrome 操作があるため前回版より長い）

### 3-3. 確認チェックリスト

| # | 確認項目 | 合格基準 |
|---|---|---|
| 1 | 日次レポートが `reports/` に生成されているか | ファイルが存在する |
| 2 | レポートが5層構造（Top Insights / Actions / Knowledge 8バケット / Raw Data / English Highlights）を満たすか | 各セクションが揃っている |
| 3 | セクション3の8プラクティス軸分類に `[C-suite: XX]` と `[業界: XX]` タグが付いているか | タグが全項目に付与されている |
| 4 | セクション4が `<details>` で折り畳まれていない（全文展開）か | 常時展開されている |
| 5 | セクション4にメール本文引用だけでなく**リンク先記事の原文引用 + 和訳**があるか | Chrome で取得成功した記事が引用されている。取得不可は URL + 📌手動確認 |
| 6 | セクション5の英単語表（10個）が存在し、例文がセクション4の引用から採られているか | 表が完成している |
| 7 | 本文の日本語に、英単語表の10単語が `（English）` 形式で併記されているか | 少なくとも初出で併記 |
| 8 | **実行者の氏名が一切含まれていないか** | `[REDACTED]` または記載なし |
| 9 | **実行者のメールアドレスが一切含まれていないか** | `[REDACTED]` または記載なし |
| 10 | 優先度Medium以上のインサイトが `insights/` に1ファイル単位で生成されているか | ファイルが存在する |
| 11 | インサイトYAMLに `practice` / `c_suite` / `sources` / `key_english_terms` が記入されているか | 全必須フィールドが存在する |
| 12 | インサイト本文に `## 📝 原文引用と和訳` セクションが存在し、英語原文が永続化されているか | セクションが存在する |
| 13 | no_accessドメインのリンクが「📌 手動確認」と明記されてスキップされているか | 明記されている |
| 14 | `claude/` ブランチ（または main）にpushされているか | GitHubで確認 |
| 15 | Chrome ブラウザの権限が「Always allow」になっているか | 次回以降の自動実行で権限プロンプトが出ない |

### 3-4. 匿名化の追加テスト

テスト実行後、以下のコマンドでレポート・インサイトに本人情報が漏れていないか機械的に確認する：

```bash
grep -ri "あなたの名前" reports/ insights/
grep -ri "あなたのメアド" reports/ insights/
grep -ri "@example.com" reports/ insights/
```

**1件でもヒットしたら、タスク定義ファイルの匿名化ルールを強化して再テスト。**

---

## STEP 4：運用開始

### 日常の確認フロー

```
毎朝（Windows PC が起動・Claude Desktop が開いている状態 → 5:30前後に自動実行）
  ↓
Gmail + Chrome 操作 + git push が自動完了
  ↓
GitHubリポジトリの reports/ を確認（日次レポート）
  ↓
レポート5層を読む
  ① Top Insights / 仮説検証
  ② Actionable Recommendations
  ③ プラクティス別ハイライト（8バケット）
  ④ Raw Data & Deep Dive（原文 + 和訳）
  ⑤ English Highlights（英単語10選）
  ↓
気になるインサイトは insights/ の個別ファイルをお気に入りに
```

### 週次メンテナンス

| 作業 | 頻度 | 方法 |
|---|---|---|
| 実行ログの確認 | 週1回 | Claude Desktop > Scheduled > タスク詳細 > 過去の実行履歴 |
| Chrome のログイン状態確認 | 週1回 | 対象5サイトに Chrome でアクセスし、ログイン済みか確認 |
| 匿名化漏れチェック | 週1回 | 上記 `grep` コマンド |
| トークン消費量の確認 | 週1回 | [claude.ai/settings/usage](https://claude.ai/settings/usage) |
| スケジュールタスクの実行時間確認 | 週1回 | 10〜30分以内か。超えている場合は Computer Use 乱用を疑う |

### 月次メンテナンス

| 作業 | 頻度 | 方法 |
|---|---|---|
| **前提検証テストの再実行** | **月1回（必須）** | [前提検証テスト手順書.md](前提検証テスト手順書.md) §7 |
| PC のスリープ設定確認 | 月1回 | Settings > "Keep computer awake" が ON か確認 |
| 送信者アドレスの見直し | 月1回 | `config/search-keywords.json` の `sender_emails` を編集 |
| ドメイン分類の見直し | 月1回 | レポート末尾の「新規ドメイン検出」メモを見て `domain-access-levels.json` を更新 |
| 古いレポートの整理 | 月1回 | 30日以上前のレポートを削除またはアーカイブ |

### 四半期メンテナンス

| 作業 | 頻度 | 方法 |
|---|---|---|
| ビジネス仮説の見直し | 四半期1回 | `config/business-hypotheses.md` を編集 |
| 失敗防止ガードレール全体の見直し | 四半期1回 | [失敗防止ガードレール.md](失敗防止ガードレール.md) の各項目を確認 |

---

## リスク評価

| リスク | 深刻度 | 緩和策 |
|---|---|---|
| **前回と同じ前提崩壊**（Chrome 経由でも取れない） | 🔴 高 | [前提検証テスト手順書.md](前提検証テスト手順書.md) で STEP −1 として事前検証。NO-GO なら代替アプローチへ |
| PC がスリープして実行されない | 🟡 中 | Settings > "Keep computer awake" ON。起動後にキャッチアップ実行 |
| Computer Use が Research Preview で不安定 | 🟡 中 | Chrome ブラウザ操作（第2層）を優先し、Computer Use は最終手段に |
| スクリーンショットに機密情報が映る | 🟡 中 | 実行前に不要なアプリを閉じる。金融・医療アプリは開かない |
| プロンプトインジェクション（Webページ経由） | 🔴 高 | [CLAUDE.md](CLAUDE.md) の絶対遵守ルールで遷移・入力を制限。信頼できるサイトのみアクセス |
| Chrome のログインセッション切れ | 🟡 中 | 週次で Chrome のログイン状態を確認 |
| 権限プロンプトで停止 | 🟡 中 | 初回の Run now で「Always allow」を必ず選択 |
| 実行時間の長期化 | 🟢 低 | Chrome 拡張の動作を確認。Computer Use へのフォールバック頻度を監視 |

---

## トラブルシューティング

| 症状 | 原因の可能性 | 対処 |
|---|---|---|
| **タスクが実行されない** | PC がスリープ / Claude Desktop が閉じている | "Keep computer awake" ON を確認。蓋を閉じていないか確認 |
| **権限プロンプトで止まっている** | 初回実行時に「Always allow」を選択していない | Scheduled > タスク詳細 > Run now で再実行し、権限を承認 |
| **Chrome で記事が取得できない** | ログインセッション切れ | Chrome で対象サイトに手動で再ログイン |
| **Chrome 拡張が無反応** | Claude in Chrome 拡張の連携切れ | 拡張を再起動、Claude Desktop を再起動 |
| **「Computer use is in use by another session」** | 別の Claude セッションが画面操作中 | そのセッションを終了してから再実行 |
| **実行が非常に遅い（30分以上）** | Computer Use（第3層）に乱用フォールバックしている | Chrome 拡張が正しく動作しているか確認。Chrome ブラウザ操作（第2層）で済むはず |
| **レポートに本人名が漏れている** | LLMが匿名化ルールを無視した | タスク定義の匿名化ルールを強化し再テスト |
| **セクション3が旧3バケットで生成される** | タスク定義・テンプレートのキャッシュ | `tasks/daily-email-scan.md` と `templates/report-template.md` が最新であることを確認 |
| **セクション4が折り畳み（`<details>`）になっている** | 旧テンプレートが参照されている | `templates/report-template.md` が現行版か確認 |
| **セクション5の英単語表が生成されない** | LLMが5層目まで辿り着いていない | タスク定義の指示の明確さを確認 |
| **インサイトに原文引用がない** | 旧 insight-universal.md が参照されている | テンプレートが現行版か確認 |
| **リンク先が全て「取得不可」** | Chrome ログインセッション切れ / Bot対策強化 | 前提検証テストを再実行。Layer 3 が通らなければ NO-GO として一旦停止 |
| **git pushが失敗** | ローカルの git 認証 / ブランチ保護 | working folder で手動 push して原因を確認 |

---

## まとめ：最終構成

```
┌────────────────────────────────────────────────────────────┐
│                    GitHub (Private)                          │
│  gmail-schedule/                                            │
│  ├── CLAUDE.md                                              │
│  ├── 前提検証テスト手順書.md                                │
│  ├── 失敗防止ガードレール.md                                │
│  ├── tasks/daily-email-scan.md                              │
│  ├── config/ (search-keywords, insight-config, ...)         │
│  ├── templates/                                             │
│  ├── docs/                                                  │
│  ├── reports/YYYY-MM-DD-*.md      ← 出力（自動生成）       │
│  └── insights/YYYY-MM-DD-NNN.md   ← 出力（自動生成）       │
└──────────────┬─────────────────────────────────┬────────────┘
               │ clone / pull                    │ push
               │                                 │
┌──────────────▼─────────────────────────────────▼────────────┐
│         ユーザーの Windows PC（Claude Desktop）                │
│         Cowork スケジュールタスク 毎朝 05:30                  │
│                                                               │
│  プロンプト: tasks/daily-email-scan.md を読んで実行せよ       │
│         │                           │                         │
│         ▼                           ▼                         │
│  Gmail コネクター             Chrome ブラウザ操作             │
│  (read-only / 第1層)          (ログイン済みセッション / 第2層)│
│  メール検索・取得             記事本文の精読                  │
│                                     │                         │
│                              Computer Use（第3層・最終手段） │
│                              画面操作で取得                   │
└───────────────────────────────────────────────────────────────┘
```

| 項目 | 内容 |
|---|---|
| **実行方式** | Cowork スケジュールタスク（Desktop local task） |
| **実行環境** | ユーザーの Windows PC（Claude Desktop が起動している必要あり） |
| **実行時刻** | 05:30（最大10分のずれあり） |
| **監視対象** | `config/search-keywords.json` の `sender_emails` |
| **メール取得** | Gmail コネクター（第1層・最速） |
| **記事取得** | Chrome ブラウザ操作（第2層）→ Computer Use（第3層）→ URL記載のみ（フォールバック） |
| **認証** | Chrome の既存ログインセッションを利用（パスワード不要） |
| **分類軸** | プラクティス8バケット × C-suite × 業界 の3軸タグ付け |
| **副次成果物** | 日次 英単語10選 + インライン併記、インサイトDB |
| **匿名化** | 実行者の氏名・メアドはレポート/インサイトに一切記載しない |
| **出力先** | ローカル `reports/` `insights/` → GitHub push |
| **見逃し補填** | PC起動後に過去7日分の最新1回を自動キャッチアップ |
| **前提検証** | 月次で [前提検証テスト手順書.md](前提検証テスト手順書.md) を再実行 |

---

## 次に進めるとしたら

1. **[前提検証テスト手順書.md](前提検証テスト手順書.md) を実行する**（最優先）
2. [初回セットアップチェックリスト.md](初回セットアップチェックリスト.md) を埋める
3. STEP 2 でスケジュールタスクを作成
4. Run now で初回テスト実行、権限を Always allow
5. 翌朝の自動実行を確認
